import { createTheme } from '@mui/material/styles';
export const myTheme=createTheme({
        palette: {
            myColor: {
              main: '#F5F5DC',
              contrastText: '#808008',
            },
          },
    });